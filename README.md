# webcam_barcode_scanner

# Python implementation
See `webcam_barcode_scanner.py`. Requires `python3`, `opencv-python`, `numpy`, and `zbar`.

# Javascript implementation
The main barcode scanning function `zbarProcessImageData()` is defined in the `zbar-processor.js` library. See `zbar-main.js` for simple webcam access in browser.
